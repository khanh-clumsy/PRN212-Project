<UserControl x:Class="TrafficViolationFeedbackSystem.ViolationTypesUpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" Text="Update Violation Type" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Violation Type ID -->
        <TextBlock Grid.Row="1" Text="ID:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Margin="100,0,0,10" Width="300" Text="{Binding UpdatedViolationType.ViolationTypeId, UpdateSourceTrigger=PropertyChanged}" IsEnabled="False"/>

        <!-- Name -->
        <TextBlock Grid.Row="2" Text="Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Margin="100,0,0,10" Width="300" Text="{Binding UpdatedViolationType.Name, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Description -->
        <TextBlock Grid.Row="3" Text="Description:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Margin="100,0,0,10" Width="300" Text="{Binding UpdatedViolationType.Description, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Standard Fine -->
        <TextBlock Grid.Row="4" Text="Standard Fine:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Margin="100,0,0,10" Width="300" Text="{Binding UpdatedViolationType.StandardFine, UpdateSourceTrigger=PropertyChanged, StringFormat=\{0:N0\} VND}"/>

        <!-- Update Button -->
        <Button Grid.Row="5" Content="Update" Margin="0,20,0,0" Padding="10,2" 
                Command="{Binding UpdateCommand}" HorizontalAlignment="Right" Width="100"/>
    </Grid>
</UserControl>
