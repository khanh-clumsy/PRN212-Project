 <UserControl x:Class="TrafficViolationFeedbackSystem.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="360.439" d:DesignWidth="988.74">
    <Grid d:DesignHeight="450" d:DesignWidth="1000">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" Text="Activity Logs" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        
        <DataGrid Grid.Row="1" ItemsSource="{Binding AuditLogs}" AutoGenerateColumns="False"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Log ID" Binding="{Binding LogId}" Width="100"/>
                <DataGridTextColumn Header="User Name" Binding="{Binding User.FullName, TargetNullValue='Unknown'}" Width="150"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="*"/>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat=\{0:dd/MM/yyyy HH:mm:ss\}}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description, TargetNullValue='No Description'}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
