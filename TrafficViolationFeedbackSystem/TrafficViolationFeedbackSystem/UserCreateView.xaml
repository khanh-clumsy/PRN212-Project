<UserControl x:Class="TrafficViolationFeedbackSystem.UserCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" Text="Create New User" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- FullName -->
        <TextBlock Grid.Row="1" Text="Full Name:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Margin="100,0,0,10" Width="300" Text="{Binding NewUser.FullName, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Email -->
        <TextBlock Grid.Row="2" Text="Email:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Margin="100,0,0,10" Width="300" Text="{Binding NewUser.Email, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Password -->
        <TextBlock Grid.Row="3" Text="Password:" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="3" Margin="100,0,0,10" Width="300" x:Name="PasswordBox" 
                     PasswordChanged="PasswordBox_PasswordChanged" />

        <!-- Phone -->
        <TextBlock Grid.Row="4" Text="Phone:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Margin="100,0,0,10" Width="300" Text="{Binding NewUser.Phone, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Address -->
        <TextBlock Grid.Row="5" Text="Address:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="5" Margin="100,0,0,10" Width="300" Text="{Binding NewUser.Address, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Role -->
        <TextBlock Grid.Row="6" Text="Role:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="6" Margin="100,0,0,10" Width="300" SelectionChanged="ComboBox_SelectionChanged">
            <ComboBoxItem Content="Citizen" Tag="Citizen"/>
             <ComboBoxItem Content="TrafficPolice" Tag="TrafficPolice"/>
             <ComboBoxItem Content="Admin" Tag="Admin"/>
        </ComboBox>

        <!-- Submit Button -->
        <Button Grid.Row="7" Content="Submit" Margin="0,20,0,0" Padding="10,2" 
                Command="{Binding SubmitCommand}" HorizontalAlignment="Right" Width="100"/>
    </Grid>
</UserControl>