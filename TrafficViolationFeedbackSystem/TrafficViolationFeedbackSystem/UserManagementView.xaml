<UserControl x:Class="TrafficViolationFeedbackSystem.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Height="405" Width="1000">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header và nút Create -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Text="Manage Accounts" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Create" Margin="10,0,0,0" Padding="10,2" Command="{Binding CreateCommand}" 
                    Background="#4CAF50" Foreground="White" BorderThickness="0"/>
            <Button Content="Update" Margin="10,0,0,0" Padding="10,2" Command="{Binding UpdateCommand}" 
                    Background="#FF9800" Foreground="White" BorderThickness="0" IsEnabled="{Binding IsUserSelected}"/>
            <Button Content="Delete" Margin="10,0,0,0" Padding="10,2" Command="{Binding DeleteCommand}" 
                    Background="#F44336" Foreground="White" BorderThickness="0" IsEnabled="{Binding IsUserSelected}"/>
        </StackPanel>

        <!-- DataGrid for Users -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Users}" AutoGenerateColumns="False" Margin="117,20,-97,0"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" SelectedItem="{Binding SelectedUser}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="User ID" Binding="{Binding UserId}" Width="50"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Password" Binding="{Binding Password}" Width="130"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>