<UserControl x:Class="TrafficViolationFeedbackSystem.Views.Admin.ManageViolationTypesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrafficViolationFeedbackSystem.Views.Admin"
        mc:Ignorable="d"
       d:DesignHeight="756" d:DesignWidth="1144">
    <Grid Background="#FFFFFF" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" 
                   Text="Quản lý loại vi phạm" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Foreground="#007BFF" 
                   Margin="20,30,0,10"
                   HorizontalAlignment="Left"/>

        <!-- Các nút Thêm, Sửa, Xóa -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,20,0,20">
            <Button x:Name="btnAddViolationType"
                    Content="➕ Thêm" 
                    Width="100" 
                    Height="35" 
                    Background="Green" 
                    Foreground="White" 
                    FontSize="14" 
                    FontWeight="SemiBold" 
                    Margin="0,0,10,0"
                    Click="btnAddViolationType_Click"/>
            <Button x:Name="btnEditViolationType"
                    Content="✏️ Sửa" 
                    Width="100" 
                    Height="35" 
                    Background="#007BFF" 
                    Foreground="White" 
                    FontSize="14" 
                    FontWeight="SemiBold" 
                    Margin="0,0,10,0"
                    Click="btnEditViolationType_Click"/>
            <Button x:Name="btnDeleteViolationType"
                    Content="🗑️ Xóa" 
                    Width="100" 
                    Height="35" 
                    Background="#FF6B6B" 
                    Foreground="White" 
                    FontSize="14" 
                    FontWeight="SemiBold" 
                    Margin="0,0,10,0"
                    Click="btnDeleteViolationType_Click"/>
        </StackPanel>

        <!-- Danh sách loại vi phạm -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="10" Margin="20">
            <DataGrid x:Name="ViolationTypesGrid" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      BorderThickness="0"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding ViolationTypeId}" Width="80"/>
                    <DataGridTextColumn Header="Tên vi phạm" Binding="{Binding Name}" Width="250"/>
                    <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Mức phạt chuẩn" Binding="{Binding StandardFine,StringFormat=\{0:N0\} VND}" Width="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
