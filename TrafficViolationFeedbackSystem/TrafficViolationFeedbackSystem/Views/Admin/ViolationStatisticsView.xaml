<UserControl x:Class="TrafficViolationFeedbackSystem.Views.Admin.ViolationStatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="360.439" d:DesignWidth="988.74">
    <Grid d:DesignHeight="450" d:DesignWidth="1000">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" Text="Violation Statistics" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
        <!-- Filter Input and Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,40,10" >
            <TextBlock Text="Filter by Plate Number:" FontSize="20" FontWeight="DemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="150" Text="{Binding FilterPlateNumber}" FontSize="18" FontWeight="DemiBold" Margin="0,0,10,0"/>
            <Button Content="🔍 Filter" Background="ForestGreen" Height="30" Width="100" Padding="10,2" Command="{Binding FilterCommand}"/>
            <Button Content="↻ Reset" Background="Red"  Width="100" Padding="10,2" Command="{Binding ResetFilterCommand}" Margin="10,0,0,0"/>
        </StackPanel>
        <!-- DataGrid for Violations -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Violations}"                  
                  AutoGenerateColumns="False" Margin="0,0,34,-63"
                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" RenderTransformOrigin="0.5,0.5">
            <DataGrid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="0.001"/>
                    <TranslateTransform/>
                </TransformGroup>
            </DataGrid.RenderTransform>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Violation ID" Binding="{Binding ViolationId}" Width="100"/>
                <DataGridTextColumn Header="Violator Name" Binding="{Binding Violator.FullName ,TargetNullValue='Unknown'}" Width="150"/>
                <DataGridTextColumn Header="PlateNumber" Binding="{Binding Report.PlateNumber}" Width="80"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Report.Description}" Width="*"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Report.Location}" Width="*"/>
                <DataGridTextColumn Header="Fine Amount" Binding="{Binding FineAmount, StringFormat=\{0:N0\} VND}" Width="120"/>
                <DataGridTextColumn Header="Date" Binding="{Binding FineDate, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="120"/>
                <DataGridTextColumn Header="Paid Status" Binding="{Binding PaidStatus, StringFormat=\{0:Paid;Unpaid\}}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>