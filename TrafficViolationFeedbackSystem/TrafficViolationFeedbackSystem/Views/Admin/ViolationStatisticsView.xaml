<UserControl x:Class="TrafficViolationFeedbackSystem.Views.Admin.ViolationStatisticsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TrafficViolationFeedbackSystem.Views.Admin"
        mc:Ignorable="d"
        d:DesignHeight="756" d:DesignWidth="1144">
    <Grid Background="#FFFFFF" Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Tiêu đề -->
        <TextBlock Grid.Row="0" 
                   Text="Thống kê vi phạm" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Foreground="#007BFF" 
                   Margin="20,30,0,10"
                   HorizontalAlignment="Left"/>

        <!-- Bộ lọc biển số -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="20,20,0,20">
            <Label Content="Lọc biển số:" FontSize="18" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,10,0"/>
            <TextBox x:Name="txtLicensePlateFilter" Width="200" Margin="0,5,0,10" FontSize="16" Padding="5" VerticalContentAlignment="Center"/>
            <Button x:Name="btnFilter" Content="🔍 Lọc" Width="87" Height="32" Margin="15,0,10,2" Click="btnFilter_Click" Background="LightGreen" Foreground="Black"/>
            <Button x:Name="btnReload" Content="↻ Tải lại" Width="87" Height="32" Margin="0,0,0,2" Click="btnReload_Click" Background="#FFD700" Foreground="Black"/>

            <Label Content="Chọn loại vi phạm:" FontSize="16" VerticalAlignment="Center" FontWeight="SemiBold" Margin="180,0,10,0"/>
            <ComboBox x:Name="cbViolationType" Width="200" Height="32" Margin="0,5,10,2" FontSize="16" VerticalContentAlignment="Center" SelectionChanged="cbViolationType_SelectionChanged"/>
        </StackPanel>

        <!-- Danh sách vi phạm -->
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="10" Margin="20">
            <DataGrid x:Name="ViolationsGrid" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      CanUserAddRows="False" 
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="All"
                      BorderThickness="0"
                      SelectionMode="Single"
                      SelectionUnit="FullRow" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID " Binding="{Binding ViolationId}" Width="50"/>
                    <DataGridTextColumn Header="Tên vi phạm" Binding="{Binding Violator.FullName}" Width="140"/>
                    <DataGridTextColumn Header="Biển số" Binding="{Binding Report.PlateNumber}" Width="100"/>
                    <DataGridTextColumn Header="Loại vi phạm" Binding="{Binding Report.ViolationType.Name}" Width="160"/>
                    <DataGridTextColumn Header="Số tiền phạt" Binding="{Binding FineAmount, StringFormat={}{0:N0} VNĐ}" Width="120"/>
                    <DataGridTextColumn Header="Ngày phạt" Binding="{Binding FineDate, StringFormat={}{0:dd/MM/yyyy   HH:mm}}" Width="140"/>
                    <DataGridTextColumn Header="Hạn thanh toán" Binding="{Binding DueDate, StringFormat={}{0:dd/MM/yyyy   HH:mm}}" Width="140"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding DisplayResult}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>