<UserControl x:Class="TrafficViolationFeedbackSystem.Views.Authentication.ForgotPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <Grid Background="#F5F5F5">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
            <TextBlock Text="Quên mật khẩu" 
                       FontSize="22" 
                       FontWeight="Bold" 
                       Margin="0,0,0,30"
                       HorizontalAlignment="Center"
                       Foreground="#333"/>

            <TextBlock Text="Nhập email để khôi phục" FontSize="14" Margin="0,0,0,5"/>
            <TextBox x:Name="txtEmail" Height="35" Padding="5" Margin="0,0,0,20" FontSize="14"/>

            <Button x:Name="btnSubmit" Content="Xác nhận" 
                    Height="40" 
                    Background="#007BFF" 
                    Foreground="White" 
                    FontSize="16"
                    FontWeight="Medium"
                    Click="btnSubmit_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0056b3"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <TextBlock x:Name="tblGoToLogin" Text="Quay lại đăng nhập"
                       FontSize="12"
                       Foreground="#007BFF"
                       HorizontalAlignment="Center"
                       Margin="15,20,0,0"
                       Cursor="Hand" MouseLeftButtonDown="tblGoToLogin_MouseLeftButtonDown"
                       />
        </StackPanel>
    </Grid>
</UserControl>
