<UserControl x:Class="TrafficViolationFeedbackSystem.Views.TrafficPolice.TrafficPoliceHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TrafficViolationFeedbackSystem.Views.TrafficPolice"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,30,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button Content="📝 Xem tất cả phản ánh mới"
                Grid.Row="0" Grid.Column="0"
                Style="{StaticResource ModernButton}"
                FontSize="22"
                Width="320" Height="120"
                Margin="20"
                Click="btnViewAllReports_Click"/>
        <Button Content="⚖️ Xem đơn kháng cáo"
                Grid.Row="0" Grid.Column="1"
                Style="{StaticResource ModernButton}"
                FontSize="22"
                Width="320" Height="120"
                Margin="20"
                Click="btnViewAppeals_Click"/>
    </Grid>
</UserControl> 