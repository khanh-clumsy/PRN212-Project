<UserControl x:Class="TrafficViolationFeedbackSystem.Views.Citizen.ReportList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrafficViolationFeedbackSystem.Views.Citizen"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <StackPanel>
            <!-- Tiêu đề -->
            <TextBlock Text="⚖️ Danh sách kháng cáo đã gửi"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="DarkRed"
                       Margin="0 0 0 10"
                       HorizontalAlignment="Center" />

            <!-- Bảng kháng cáo -->
            <DataGrid x:Name="dgAppeals"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      FontSize="14"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Kháng Cáo" Binding="{Binding AppealId}" Width="Auto"/>
                    <DataGridTextColumn Header="Loại Vi Phạm" Binding="{Binding Violation.Report.ViolationType.Name}" Width="*" />
                    <DataGridTextColumn Header="Ngày Vi Phạm" Binding="{Binding Violation.FineDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="Auto"/>
                    <DataGridTextColumn Header="Nội Dung" Binding="{Binding Content}" Width="2*"/>
                    <DataGridTextColumn Header="Ngày Gửi" Binding="{Binding SubmitDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="Auto"/>
                    <DataGridTextColumn Header="Kết Quả" Binding="{Binding ResultText}" Width="Auto"/>

                    <DataGridTemplateColumn Header="Thao tác" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0">
                                    <!-- Nút Sửa -->
                                    <Button Content="Sửa"
                                            Padding="5,2"
                                            Background="DodgerBlue"
                                            Foreground="White"
                                            FontWeight="Bold"
                                            Click="EditAppeal_Click"
                                            Tag="{Binding AppealId}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Result}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Result}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Result}" Value="{x:Static sys:String.Empty}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- Nút Xóa -->
                                    <Button Content="Xóa"
                                            Padding="5,2"
                                            Background="Crimson"
                                            Foreground="White"
                                            FontWeight="Bold"
                                            Click="DeleteAppeal_Click"
                                            Tag="{Binding AppealId}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Result}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Result}" Value="">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Result}" Value="{x:Static sys:String.Empty}">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
