<UserControl x:Class="TrafficViolationFeedbackSystem.Views.Citizen.PayFineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel>
            <TextBlock Text="💳 Danh sách khoản phạt cần nộp"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="#1C1C1C"
                       Margin="0 0 0 20"/>

            <DataGrid x:Name="dgFines"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      Margin="0 0 0 10"
                      ItemsSource="{Binding FineList}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã Vi Phạm" Binding="{Binding ViolationID}" Width="100"/>
                    <DataGridTextColumn Header="Số tiền" Binding="{Binding Amount, StringFormat='{}{0:N0} đ'}" Width="150"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding DisplayResult}" Width="150"/>
                    <DataGridTextColumn Header="Ngày thanh toán" Binding="{Binding PaymentDate, StringFormat='dd/MM/yyyy'}" Width="120"/>
                    <DataGridTemplateColumn Header="Hành động" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Thanh toán"
                                        Click="BtnPay_Click"
                                        Tag="{Binding}"
                                        Background="#2196F3"
                                        Foreground="White"
                                        Padding="4 2"
                                        Margin="4 0"
                                        IsEnabled="{Binding Status, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
